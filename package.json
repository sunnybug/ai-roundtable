{
  "name": "ai-roundtable",
  "version": "0.1.5",
  "description": "让多个 AI 助手围桌讨论，交叉评价，深度协作",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:e2e": "playwright test tests/e2e.spec.ts",
    "test:e2e:headed": "playwright test tests/e2e.spec.ts --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18"
  }
}
